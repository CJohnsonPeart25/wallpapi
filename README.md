# wallpapi

A personal tool for finding wallpapers you actually like.

See `CONTEXT.md` for the domain vocabulary and `AGENTS.md` for the stack and invariants.

```
uv run uvicorn wallpapi.web.app:app --host 127.0.0.1 --port 8000
```
